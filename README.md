# valentine.io 💖

A cute, mobile-friendly Valentine “proposal” single-page site for GitHub Pages:
**“Will you be my Valentine?”** with playful **Yes** / evasive **No** buttons.

## Files
- `index.html` — page structure + inline SVG illustration
- `styles.css` — theme, layout, animations, accessibility focus styles
- `script.js` — NO-dodging logic, microcopy rotation, YES celebration burst

## Run locally
Option A (fastest): just open `index.html` in your browser.

Option B (recommended): run a tiny local server.

### macOS / Linux (Python)
```bash
python3 -m http.server 5500
